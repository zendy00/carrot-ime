# CarrotIME — 입력 상태 표시기 (IME Indicator)

윈도우에서, 현재 입력기가 한글인지 영문인지를 텍스트 입력 커서 바로 옆에 항상 보여주는 프로그램. macOS의 입력 소스 표시기를 모든 윈도우 앱에서 재현하는 것이 목표.

## Language

**인디케이터 (Indicator)**:
현재 입력기 상태(한/영)를 나타내는, 화면에 떠다니는 작은 시각 표시.
_Avoid_: 툴팁, 배지, 팝업 — 문맥상 혼용하지 말 것.

**캐럿 (Caret)**:
글자가 실제로 찍히는 텍스트 커서(보통 깜빡이는 세로 막대). 인디케이터가 따라다녀야 하는 대상.
_Avoid_: 커서(모호함), 입력 커서만 단독 사용

**마우스 포인터 (Mouse Pointer)**:
마우스로 움직이는 화살표 포인터. 캐럿과 명확히 다르다. 기성 무료툴 대부분은 여기에 표시하지만, 이 프로젝트는 캐럿을 따라간다.
_Avoid_: 커서, 마우스 커서

**입력기 (IME)**:
한글처럼 여러 자모를 조합해 문자를 만드는 입력 방식 엔진.
_Avoid_: 키보드, 언어

**입력 상태 (Input State)**:
인디케이터가 구분해 보여주는 현재 입력 상태. 값은 세 가지 — **한글**(한글 IME가 한글 조합 모드), **영문**(영문 모드·영어 레이아웃 등 그 외 알파벳 입력), **기타 IME**(일본어·중국어 등 다른 입력기). 키보드 레이아웃 전환과 한글 IME 내부의 한/영 전환을 모두 반영한다.
_Avoid_: 한·영 상태(3번째 값을 빠뜨림), 언어 설정, 키보드 상태
